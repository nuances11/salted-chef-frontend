import { ZipcodeRangeDirective } from './zipcode-range.directive';

describe('ZipcodeRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ZipcodeRangeDirective();
    expect(directive).toBeTruthy();
  });
});
